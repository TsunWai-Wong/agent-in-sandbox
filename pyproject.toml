[project]
name = "sandbox-try"
version = "0.1.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arize-phoenix-otel>=0.17.1",
    "datasets>=5.0.1",
    "elasticsearch>=9.5.0",
    "fastapi>=0.115.14",
    "genai-bench>=0.0.5",
    "google-genai>=2.19.0",
    "lm-eval[api]>=0.4.12",
    "mlx>=0.30.6",
    "mlx-lm>=0.31.3",
    "mlx-vlm>=0.6.15",
    "onnxruntime>=1.20.0",
    "openai>=3.1.0",
    "openinference-instrumentation>=0.1.57",
    "openinference-instrumentation-openai>=0.1.54",
    "opentelemetry-api>=1.44.0",
    "outlines>=1.0",
    "playwright>=1.62.0",
    # Binary build so a plain `uv sync` needs no libpq/compiler on the host.
    "psycopg[binary]>=3.2",
    "pydantic>=2.13.4",
    # agent.conversation, agent.llm_service and monitoring.tracing all call
    # load_dotenv() at construction.
    "python-dotenv>=1.0.1",
    "uvicorn>=0.35.0",
]
